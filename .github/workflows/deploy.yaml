# https://vitepress.dev/guide/deploy#github-pages

name: Deploy VitePress site to Pages

on:
  push:
    branches: [main]
  workflow_run:
    workflows: ['Update Winning Numbers']
    branches: [main]
    types:
      - completed

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v3
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: pnpm
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Install dependencies
        run: pnpm install
      - name: Build with VitePress
        run: pnpm build
        env:
          GOOGLE_ANALYTICS_ID: ${{ secrets.GOOGLE_ANALYTICS_ID }}
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  notify:
    runs-on: ubuntu-latest
    needs: deploy
    if: always()
    steps:
      - name: Get commit message
        id: commit
        env:
          COMMIT_MSG: ${{ github.event.head_commit.message }}
        run: |
          MSG=$(echo "$COMMIT_MSG" | head -n 1)
          # Escape HTML special chars
          MSG=$(echo "$MSG" | sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g')
          echo "message=$MSG" >> $GITHUB_OUTPUT
      - name: Notify Telegram (Success)
        if: needs.deploy.result == 'success'
        uses: appleboy/telegram-action@v1.0.1
        with:
          token: ${{ secrets.TELEGRAM_TOKEN }}
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          format: html
          disable_web_page_preview: true
          message: |
            <b> ✅ 部署成功</b>

            📦 <b>專案: </b> <code>${{ github.repository }}</code>
            🌿 <b>分支: </b> <code>${{ github.ref_name }}</code>
            👤 <b>觸發: </b> <code>${{ github.actor }}</code>
            📝 ${{ steps.commit.outputs.message }}

            👉 <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">詳細</a>
      - name: Notify Telegram (Failure)
        if: needs.deploy.result == 'failure'
        uses: appleboy/telegram-action@v1.0.1
        with:
          token: ${{ secrets.TELEGRAM_TOKEN }}
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          format: html
          disable_web_page_preview: true
          message: |
            <b> ❌ 部署失敗</b>

            📦 <b>專案: </b> <code>${{ github.repository }}</code>
            🌿 <b>分支: </b> <code>${{ github.ref_name }}</code>
            👤 <b>觸發: </b> <code>${{ github.actor }}</code>
            📝 ${{ steps.commit.outputs.message }}

            👉 <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">詳細</a>
